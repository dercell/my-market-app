server.port=${SERVICE_PORT:8090}

#custom properties
payment-service.current-balance=${BALANCE:30000}

spring.security.oauth2.resourceserver.jwt.issuer-uri=${KEYCLOAK_ISSUER_URI:http://localhost:8080/realms/market-app}